const test = QUnit.test;

QUnit.module('create header html');

test('dynamically makes header html', function(assert) {
    const user = {
        
    };
    
    
    assert.htmlEqual(true, false);
});
